// Cynhyrchwyd y ffeil hon yn awtomatig. PEIDIWCH Â MODIWL
// This file is automatically generated. DO NOT EDIT

// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-ignore: Unused imports
import { Create as $Create } from "@wailsio/runtime";

/**
 * APIFieldDrift is one column where the host's stored value differs
 * from the source's current value. Stored may be nil (the column was
 * added to Map[] after the form was saved); Current may be nil (the
 * source-side field was deleted/cleared).
 */
export class APIFieldDrift {
    "key": string;
    "stored": any;
    "current": any;

    /** Creates a new APIFieldDrift instance. */
    constructor($$source: Partial<APIFieldDrift> = {}) {
        if (!("key" in $$source)) {
            this["key"] = "";
        }
        if (!("stored" in $$source)) {
            this["stored"] = null;
        }
        if (!("current" in $$source)) {
            this["current"] = null;
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new APIFieldDrift instance from a string or object.
     */
    static createFrom($$source: any = {}): APIFieldDrift {
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        return new APIFieldDrift($$parsedSource as Partial<APIFieldDrift>);
    }
}

/**
 * APIFieldRefetchResultDTO mirrors APIFieldRefetchResult but carries
 * the same Kind/Message error fields as APIFieldRowResult so the
 * frontend can branch uniformly. On success Row + Drift are populated;
 * on error, both are zero and Kind is non-empty.
 */
export class APIFieldRefetchResultDTO {
    "row"?: { [_ in string]?: any };
    "drift"?: APIFieldDrift[];
    "kind"?: string;
    "message"?: string;

    /** Creates a new APIFieldRefetchResultDTO instance. */
    constructor($$source: Partial<APIFieldRefetchResultDTO> = {}) {

        Object.assign(this, $$source);
    }

    /**
     * Creates a new APIFieldRefetchResultDTO instance from a string or object.
     */
    static createFrom($$source: any = {}): APIFieldRefetchResultDTO {
        const $$createField0_0 = $$createType0;
        const $$createField1_0 = $$createType2;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("row" in $$parsedSource) {
            $$parsedSource["row"] = $$createField0_0($$parsedSource["row"]);
        }
        if ("drift" in $$parsedSource) {
            $$parsedSource["drift"] = $$createField1_0($$parsedSource["drift"]);
        }
        return new APIFieldRefetchResultDTO($$parsedSource as Partial<APIFieldRefetchResultDTO>);
    }
}

/**
 * APIFieldRowResult is the Wails-friendly response shape for
 * FetchAPIFieldRow. Errors are surfaced as a stable Kind string so
 * the frontend can branch without parsing free-form messages.
 * 
 * Kinds:
 *   - ""                     — success; Row is the projected row
 *   - "template-not-found"   — sourceTemplate does not exist
 *   - "collection-disabled"  — sourceTemplate exists but collection-mode is off
 *   - "guid-not-found"       — guid is not present in that collection
 *   - "internal"             — anything else (Message carries the detail)
 */
export class APIFieldRowResult {
    "row"?: { [_ in string]?: any };
    "kind"?: string;
    "message"?: string;

    /** Creates a new APIFieldRowResult instance. */
    constructor($$source: Partial<APIFieldRowResult> = {}) {

        Object.assign(this, $$source);
    }

    /**
     * Creates a new APIFieldRowResult instance from a string or object.
     */
    static createFrom($$source: any = {}): APIFieldRowResult {
        const $$createField0_0 = $$createType0;
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        if ("row" in $$parsedSource) {
            $$parsedSource["row"] = $$createField0_0($$parsedSource["row"]);
        }
        return new APIFieldRowResult($$parsedSource as Partial<APIFieldRowResult>);
    }
}

/**
 * TemplateSummary is the public projection of an index template row.
 * `Filename` is the YAML name (e.g. "basic.yaml"); `Stem` is the
 * extension-less form (e.g. "basic") which matches the URL slug used
 * by the original Formidable wiki ("/template/basic").
 */
export class TemplateSummary {
    "stem": string;
    "filename": string;
    "name": string;
    "itemField"?: string;
    "guidField"?: string;
    "tagsField"?: string;
    "hasMarkdownTemplate": boolean;
    "enableCollection": boolean;

    /** Creates a new TemplateSummary instance. */
    constructor($$source: Partial<TemplateSummary> = {}) {
        if (!("stem" in $$source)) {
            this["stem"] = "";
        }
        if (!("filename" in $$source)) {
            this["filename"] = "";
        }
        if (!("name" in $$source)) {
            this["name"] = "";
        }
        if (!("hasMarkdownTemplate" in $$source)) {
            this["hasMarkdownTemplate"] = false;
        }
        if (!("enableCollection" in $$source)) {
            this["enableCollection"] = false;
        }

        Object.assign(this, $$source);
    }

    /**
     * Creates a new TemplateSummary instance from a string or object.
     */
    static createFrom($$source: any = {}): TemplateSummary {
        let $$parsedSource = typeof $$source === 'string' ? JSON.parse($$source) : $$source;
        return new TemplateSummary($$parsedSource as Partial<TemplateSummary>);
    }
}

// Private type creation functions
const $$createType0 = $Create.Map($Create.Any, $Create.Any);
const $$createType1 = APIFieldDrift.createFrom;
const $$createType2 = $Create.Array($$createType1);
